# =============================================================================
# Git Presented - Configuration
# =============================================================================
# Copy this file to .env and customize the values for your setup.
# =============================================================================

# -----------------------------------------------------------------------------
# Git Repository Configuration
# -----------------------------------------------------------------------------

# Path to the Git repository to present
# Leave empty to use the current directory (this project)
# Can be absolute path or relative to this project
# Examples:
#   GIT_REPO_PATH=                              # Use current directory
#   GIT_REPO_PATH=../my-project                 # Relative path
#   GIT_REPO_PATH=/home/user/projects/my-app   # Absolute path
GIT_REPO_PATH=

# File exclusion patterns (comma-separated glob patterns)
# Files matching these patterns will be hidden from diffs in presentations
# Default: vendor/*, *.lock, node_modules/*, etc.
GIT_EXCLUDE_PATTERNS="vendor/*,*.lock,composer.lock,package-lock.json,yarn.lock,node_modules/*,.gitignore,.env*,CLAUDE.md"

# -----------------------------------------------------------------------------
# Site Configuration
# -----------------------------------------------------------------------------

# Site name displayed in header and browser title
SITE_NAME="Git Presentations"

# Author name (optional, shown in presentation metadata)
SITE_AUTHOR=

# -----------------------------------------------------------------------------
# Theme and Display Settings
# -----------------------------------------------------------------------------

# Default color theme
# Options: light, dark, presentation, schiphol, laravel, symfony, microsoft,
#          catppuccin-latte, catppuccin-frappe, catppuccin-macchiato, catppuccin-mocha
DEFAULT_THEME=light

# Default display mode
# Options:
#   browser - Compact view optimized for reading on screen
#   beamer  - Large fonts optimized for projection/presenting
DEFAULT_MODE=beamer

# -----------------------------------------------------------------------------
# Diff Display Settings
# -----------------------------------------------------------------------------

# Maximum number of changed files before treating a commit as a "bulk change"
# Bulk changes (like framework installs) hide individual diffs to keep
# presentations focused. Set higher if you want to show more files.
MAX_FILES_FOR_INLINE_DIFF=10
